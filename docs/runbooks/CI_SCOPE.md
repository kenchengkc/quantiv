# CI scope

Critical CI prioritizes deterministic correctness of source, data contracts, model-control logic, security boundaries, and deployable artifacts. Expensive or cosmetic browser checks should remain visible but should not obscure failures in those core gates.
