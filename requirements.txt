# Quantiv — Python deps for GitHub Actions data refresh and local scripts
# (`daily-refresh.yml` uses this file). The FastAPI Docker image installs
# apps/backend/requirements.txt separately; overlap is intentional but pins
# should stay compatible.

# Core Data & Database
SQLAlchemy>=2.0.0
psycopg2-binary>=2.9
duckdb>=1.0.0
python-dotenv>=1.0.0

# Data Processing
pandas>=2.2.0
pyarrow>=15.0.0
polars>=0.20.0
numpy>=1.24.0
scipy>=1.11.0

# Machine Learning
scikit-learn>=1.3.0
lightgbm>=4.1.0
joblib>=1.3.0
optuna>=3.5.0

# HTTP
requests>=2.31.0

# Web API & Validation
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.13.4

# Caching & Storage
redis>=5.0.0