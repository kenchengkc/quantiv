# Quantiv Backend runtime dependencies.
# Docker installs this file into the Railway production image. CI installs
# pytest/ruff separately, so development tools do not belong in this surface.

# FastAPI and async web framework
# Pin: FastAPI releases changed internal router objects used by SlowAPI route
# exemption wiring; keep the tested serving pair explicit.
fastapi==0.135.2
starlette==1.0.0
uvicorn[standard]>=0.24.0
httpx>=0.25.0
pydantic>=2.5.0
python-dotenv>=1.0.0
websockets>=12.0

# Database and caching
asyncpg>=0.29.0
# Backend tests import scripts/import_recent_to_postgres.py; keep psycopg2 here
# until that test dependency is split into a dedicated CI requirements group.
psycopg2-binary>=2.9.0
redis>=5.0.0

# Data processing
pandas>=2.2.0,<3
numpy>=1.24.0,<2
duckdb>=1.0.0
requests>=2.31.0
pyarrow>=15.0.0

# Cloudflare R2 model materialization. R2 is S3-compatible and boto3 uses a
# custom endpoint URL; required for signed champion activation at startup.
boto3>=1.34.0
cryptography>=43.0.0

# Rate limiting
slowapi==0.1.10

# Structured production logging
structlog>=23.2.0
