# Quantiv ML development/training environment.
#
# Production serving installs apps/ml as a package from pyproject.toml and uses
# apps/backend/requirements.txt for the API process. Keep this file focused on
# training, research scripts, and their tests; do not add backend/web or legacy
# object-store dependencies here.

# Core data
pandas>=2.0.0,<3
numpy>=1.24.0,<2
pyarrow>=15.0.0
duckdb>=1.0.0

# ML / model control
scikit-learn>=1.3.0
lightgbm>=4.0.0
optuna>=3.3.0
cryptography>=43.0.0

# Pipeline IO / config
requests>=2.31.0
python-dotenv>=1.0.0
pydantic>=2.5.0

# Optional training-path persistence
psycopg2-binary>=2.9.0
sqlalchemy>=2.0.0
redis>=5.0.0

# Research plotting used by scripts/research/walk_forward.py
matplotlib>=3.7.0

# Tests / lint
pytest>=7.4.0
pytest-asyncio>=0.21.0
# Match CI/backend lint semantics; avoid floating into new rule sets implicitly.
ruff==0.14.4
