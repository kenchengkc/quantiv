# ---------- Frontend (Next.js) ----------
NODE_ENV=
NEXT_PUBLIC_APP_URL=
NEXT_PUBLIC_API_URL=

# ---------- Backend / ML ----------
# Postgres (dev example uses localhost)
POSTGRES_HOST=
POSTGRES_PORT=5432
POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=

# Or a single URL (prefer this in production)
DATABASE_URL=

# Upstash Redis
# Redis (TCP/TLS) URL — works with redis-py, ioredis
REDIS_URL=
# OR Upstash REST (for @upstash/redis)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# External APIs
POLYGON_API_KEY=
FMP_API_KEY=

# ML / data
PARQUET_ROOT=
DATA_DIR=./data
DUCKDB_PATH=./quantiv.duckdb
DATA_BACKEND=
